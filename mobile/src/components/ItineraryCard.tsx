import React from "react";
import { StyleSheet, Text, View } from "react-native";
import { colors } from "../theme/colors";
type Item = { time_slot: string; title: string; description?: string; estimated_minutes?: number; cultural_note?: string; travel_tip?: string };
export function ItineraryCard({ item }: { item: Item }) { return <View style={styles.card}><View style={styles.time}><Text style={styles.slot}>{item.time_slot.toUpperCase()}</Text><Text style={styles.duration}>{item.estimated_minutes || 90} min</Text></View><Text style={styles.title}>{item.title}</Text><Text style={styles.description}>{item.description}</Text>{item.cultural_note && <Text style={styles.note}>Culture · {item.cultural_note}</Text>}{item.travel_tip && <Text style={styles.tip}>Tip · {item.travel_tip}</Text>}</View>; }
const styles = StyleSheet.create({ card: { backgroundColor: colors.white, borderRadius: 15, padding: 15, borderWidth: 1, borderColor: colors.line, marginBottom: 10 }, time: { flexDirection: "row", justifyContent: "space-between" }, slot: { color: colors.saffron, fontWeight: "700", fontSize: 10, letterSpacing: 1 }, duration: { color: colors.muted, fontSize: 10 }, title: { color: colors.charcoal, fontWeight: "700", fontSize: 15, marginTop: 9 }, description: { color: colors.muted, fontSize: 12, lineHeight: 18, marginTop: 5 }, note: { color: colors.indigo, backgroundColor: colors.indigoSoft, fontSize: 10, lineHeight: 15, padding: 8, borderRadius: 8, marginTop: 10 }, tip: { color: colors.emerald, fontSize: 10, marginTop: 9 } });
