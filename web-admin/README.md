# Web admin

The admin dashboard manages destination twins, cultural content, events,
crowd/safety alerts, and local experiences. The working showcase dashboard is
currently available from the root app's **Admin showcase** switch.

Use this folder for the production React/Next.js admin surface:

```bash
npm install
npm run dev
```
